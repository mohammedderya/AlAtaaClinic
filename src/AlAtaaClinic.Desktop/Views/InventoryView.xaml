<UserControl x:Class="AlAtaaClinic.Desktop.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:AlAtaaClinic.Desktop.Controls"
             xmlns:l="clr-namespace:AlAtaaClinic.Desktop.Common">
    <ScrollViewer Background="{DynamicResource Brush.BackgroundAlt}"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="28,24,28,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Text="{l:Loc PageTitle.Inventory}" Style="{StaticResource Text.PageTitle}" />
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <controls:SearchBar Text="{Binding SearchText}" SearchCommand="{Binding SearchCommand}" />
                    <Button Grid.Column="2" Content="{l:Loc Common.New}" Style="{StaticResource Button.Primary}" Command="{Binding AddCommand}" />
                    <Button Grid.Column="4" Content="{l:Loc Common.Edit}" Style="{StaticResource Button.Secondary}" Command="{Binding EditCommand}" />
                    <Button Grid.Column="6" Content="{l:Loc Common.Deactivate}" Style="{StaticResource Button.Danger}" Command="{Binding DeleteCommand}" />
                    <Button Grid.Column="8" Content="{l:Loc Common.Print}" Style="{StaticResource Button.Secondary}" Command="{Binding PrintCommand}" />
                </Grid>
            </Border>
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,16,0,0">
                <DataGrid ItemsSource="{Binding Medicines}" SelectedItem="{Binding SelectedMedicine}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{l:Loc Common.Code}" Binding="{Binding MedicineCode}" Width="130" />
                        <DataGridTextColumn Header="{l:Loc Common.GenericName}" Binding="{Binding GenericName}" Width="*" />
                        <DataGridTextColumn Header="{l:Loc Common.TradeName}" Binding="{Binding TradeName}" Width="220" />
                        <DataGridTextColumn Header="{l:Loc Common.Unit}" Binding="{Binding Unit}" Width="120" />
                        <DataGridCheckBoxColumn Header="{l:Loc Common.Active}" Binding="{Binding IsActive}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
