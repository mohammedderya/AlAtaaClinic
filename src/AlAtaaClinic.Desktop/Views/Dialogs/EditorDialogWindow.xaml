<Window x:Class="AlAtaaClinic.Desktop.Views.Dialogs.EditorDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AlAtaaClinic.Desktop.ViewModels.Dialogs"
        xmlns:dialogs="clr-namespace:AlAtaaClinic.Desktop.Views.Dialogs"
        Width="780"
        MinWidth="640"
        MinHeight="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource Brush.Background}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:BranchEditorViewModel}">
            <dialogs:BranchEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DepartmentEditorViewModel}">
            <dialogs:DepartmentEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PatientEditorViewModel}">
            <dialogs:PatientEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AppointmentEditorViewModel}">
            <dialogs:AppointmentEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MedicineEditorViewModel}">
            <dialogs:MedicineEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PaymentEditorViewModel}">
            <dialogs:PaymentEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CharityCaseEditorViewModel}">
            <dialogs:CharityCaseEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:VisitEditorViewModel}">
            <dialogs:VisitEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InvoiceEditorViewModel}">
            <dialogs:InvoiceEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StaffMemberEditorViewModel}">
            <dialogs:StaffMemberEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DoctorEditorViewModel}">
            <dialogs:DoctorEditorView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource Brush.PrimaryGradient}" Padding="28,22,28,18">
            <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}"
                       FontFamily="{DynamicResource Font.App}"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="White" />
        </Border>
        <Border Grid.Row="1" Background="{DynamicResource Brush.Surface}" Padding="28,28,28,24">
            <ContentControl Content="{Binding}" />
        </Border>
        <Border Grid.Row="2" Background="{DynamicResource Brush.BackgroundAlt}"
                Padding="28,16"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{DynamicResource Brush.Danger}"
                           FontFamily="{DynamicResource Font.Body}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" />
                <Button Grid.Column="1"
                        Content="Cancel"
                        Style="{StaticResource Button.Secondary}"
                        Command="{Binding CancelCommand}" />
                <Button Grid.Column="3"
                        Content="Save"
                        Style="{StaticResource Button.Primary}"
                        Command="{Binding SaveCommand}" />
            </Grid>
        </Border>
    </Grid>
</Window>
