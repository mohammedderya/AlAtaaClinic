<UserControl x:Class="AlAtaaClinic.Desktop.Controls.BarChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Label}" Style="{StaticResource Text.Muted}" VerticalAlignment="Center" />
                    <Grid Grid.Column="1" Height="12" VerticalAlignment="Center">
                        <Border Background="{DynamicResource Brush.SurfaceAlt}" CornerRadius="6" />
                        <Border Background="{DynamicResource Brush.Primary}"
                                CornerRadius="6"
                                HorizontalAlignment="Left"
                                Width="{Binding Percent}" />
                    </Grid>
                    <TextBlock Grid.Column="2"
                               Text="{Binding Value}"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center" />
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
